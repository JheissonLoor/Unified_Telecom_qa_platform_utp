# Trivy

Ultima ejecucion: 2026-06-19. Resultado del escaneo de vulnerabilidades,
secretos y misconfiguraciones: 0 criticas, 0 altas, 0 secretos y 0
misconfiguraciones fallidas.

El detalle local se genera en `filesystem.json` y se excluye de Git. Ejecutar:
`powershell -File scripts/run_trivy.ps1`.
